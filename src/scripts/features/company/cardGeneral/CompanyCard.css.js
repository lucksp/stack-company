import styled from "styled-components";

export default styled.div`
  font-size: 14px;
  .header,
  .footer {
    padding: 8px 16px;
  }
  .detail {
    margin-bottom: 8px;
    text-transform: capitalize;
  }
  .title {
    font-weight: 600;
  }
`;
